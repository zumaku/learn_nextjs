import "server-only"

export function ServerFuncionExample() {
    console.log(`
        Koneksi ke database.
        Fetching data dari external database.
        dll.
    `)
    return "Ini Server Function"
}